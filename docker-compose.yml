networks:
    private-inet:
        external: true

services:
    sipelan-fe:
        build: .
        ports:
            - "4173:4173"
        container_name: sipelan-fe
        restart: unless-stopped
